<h1><%= t 'cp.manage_forums' %></h1>

<p>
  <%= link_to t('cp.forums.add'), manage_forums_add_form_path,
              :class => 'btn btn-primary' %>
</p>

<table class="table table-condensed">
  <tr>
    <th><%= t 'cp.forums.forum_title' %></th>
    <th><%= t 'cp.forums.forum_desc' %></th>
    <th><%= t 'cp.forums.sorting_id' %></th>
    <th></th>
  </tr>
  <% @forums.each do |forum| %>
    <tr>
      <td><%= forum['title'] %></td>
      <td><%= forum['desc'] %></td>
      <td><%= forum['sort_by'] || '0' %></td>
      <td>
        <%= button_to t('cp.forums.delete_forum'),
                      main_app.manage_forums_delete_path(:forum_id => forum['id']),
                      :onclick => "return confirm('#{t('cp.forum.confirm_delete')}\\n#{forum['title']}')",
                      :method => 'delete',
                      :class => 'btn btn-xs btn-danger' %>
      </td>
    </tr>
  <% end %>
</table>

<p class="bg-danger">
  TODO: <%= link_to t('cp.forums.apply_sorting'),
              '#',
              :class => 'btn btn-xs btn-default' %>
</p>
